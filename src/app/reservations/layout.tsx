export const metadata = {
  title: "Reserve a Table · Gold House",
  description: "Book your table at OKÜ, CATCH, or TERRACE at Gold House in Casco Viejo.",
};

export default function ReservationsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
