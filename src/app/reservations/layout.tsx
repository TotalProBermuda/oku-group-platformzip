export const metadata = {
  title: "Reserve a Table · Gold House",
  description: "Book your table at Gold House, Casco Viejo — fine dining, nightlife, terrace and private events.",
};

export default function ReservationsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
